# Steps Taken to Complete This Task

## 1. Clone the Repository
Cloned `HiLleywyn/TestOne` from GitHub into the sandboxed workspace using the `git clone` tool.

## 2. Create a New Branch
Created branch `Ididit` off `main` using `git checkout` with `create=true`.

## 3. Write Documentation
Created this `STEPS.md` file documenting every action taken.

## 4. Commit Changes
Staged and committed `STEPS.md` with a descriptive commit message.

## 5. Push to Remote
Pushed the `Ididit` branch to the remote origin.

## 6. Open Pull Request
